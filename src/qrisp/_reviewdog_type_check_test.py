"""Temporary test to verify the mypy reviewdog check passes as expected."""


def _type_check_ok(x: int) -> str:
    """Temporary helper used to verify the mypy reviewdog check passes."""
    return str(x)
